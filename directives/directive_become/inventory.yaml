all:
  children:
    localhost:
      hosts:
        local:
          ansible_host: $(hostname -i)
          ansible_connection: local
          ansible_port: 22
          ansible_user: root
    web:
      hosts:
        web1:
          ansible_host: "{{ lookup('env', 'WEB1_IP') }}"
          ansible_connection: ssh
          ansible_port: 22
          ansible_user: web_user
          ansible_ssh_private_key_file: ~/.ssh/web_user
    db:
      hosts:
        db1:
          ansible_host: "{{ lookup('env', 'DB1_IP') }}"
          ansible_connection: ssh
          ansible_port: 22
          ansible_user: db_user
          ansible_ssh_private_key_file: ~/.ssh/db_user
    web_db:
      children:
        web:
        db:
