- name: Access the host vars & group vars 
  hosts: all
  tasks:
    - debug: 
        var: hostvars[inventory_hostname].comment_message
    - debug:
        var: hostvars[inventory_hostname].support_email
